[defaults]
inventory = inventory.ini
remote_user = ubuntu
private_key_file = ~/.ssh/id_rsa  # SSH 키 경로 (로컬 환경에 맞게 변경)
host_key_checking = False
retry_files_enabled = False
timeout = 30
pipelining = True
forks = 10