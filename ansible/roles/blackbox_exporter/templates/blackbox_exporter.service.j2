[Unit]
Description=Blackbox Exporter
After=network.target

[Service]
ExecStart=/bin/bash -c '/usr/local/bin/blackbox_exporter-0.25.0.linux-amd64/blackbox_exporter --config.file=/etc/blackbox_exporter/config.yml'
Restart=always

[Install]
WantedBy=default.target
